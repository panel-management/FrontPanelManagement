services:
  panel:
    build: .
    image: panel
    container_name: panel-management
    ports:
      ports:
        - '80:80'
        - '443:443/tcp'
        - '443:443/udp'
    restart: always
    volumes:
      - /etc/letsencrypt:/etc/nginx/ssl:ro
    env_file:
      - .env
